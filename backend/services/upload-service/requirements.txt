# Upload service runtime deps.
# NOTE: the shared `contracts` package is installed separately (local path,
# not PyPI) — see the Dockerfile / README, not this file.

fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
python-multipart>=0.0.12        # REQUIRED by FastAPI to parse file/form uploads
google-cloud-storage>=2.18,<4.0
confluent-kafka>=2.5,<3.0
python-dotenv>=1.0,<2.0
sqlalchemy