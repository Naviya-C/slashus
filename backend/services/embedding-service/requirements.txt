# Embedding service runtime deps.
# NOTE: `contracts` is installed separately (local path) — see the Dockerfile.
# NOTE: torch is installed CPU-only in the Dockerfile (before this file) to keep
#       the image from pulling ~2GB of CUDA libs you don't need on a CPU VM.

# health-check API (see run_all.py, api.py)
fastapi>=0.115,<1.0
uvicorn>=0.30,<1.0

confluent-kafka>=2.5,<3.0
qdrant-client>=1.10,<2.0
sentence-transformers>=3.0        # BGE-M3 via SentenceTransformer
python-dotenv>=1.0,<2.0
google-auth>=2.30,<3.0            # ADC token for Managed Kafka OAUTHBEARER (build_conf path)
