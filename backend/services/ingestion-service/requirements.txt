# Ingestion service runtime deps.
# NOTE: the shared `contracts` package is installed separately (local path,
# not PyPI) — handled in the Dockerfile, not here: pip install /app/shared/contracts

# health-check API (see src/run_all.py, src/ingestion/api/main.py)
fastapi>=0.115,<1.0
uvicorn>=0.30,<1.0

# messaging + cloud
confluent-kafka>=2.5,<3.0
google-cloud-storage>=2.18,<4.0
google-auth>=2.30,<3.0          # ADC token for Managed Kafka OAUTHBEARER (build_conf path)

# PDF extraction
PyMuPDF>=1.24                   # imported as `fitz`
pdfplumber>=0.11

# OCR (needs the system `tesseract-ocr` binary — see Dockerfile)
pytesseract>=0.3.10
Pillow>=10.0

# LLM enrichment — current unified SDK (`from google import genai`)
google-genai>=0.3