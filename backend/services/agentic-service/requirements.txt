# LLM — Qwen via Alibaba Model Studio
dashscope>=1.20

# Orchestration — the agent flow is a LangGraph StateGraph (agent/graph.py).
# The checkpointer is what earns it: state is persisted per session thread, so
# a turn that crashes mid-loop resumes instead of restarting, and conversation
# state survives without being hand-rolled into Redis.
langgraph>=0.2.60
langgraph-checkpoint>=2.0
# Redis-backed checkpointer. Falls back to MemorySaver when REDIS_URL is unset,
# which is fine for one replica and wrong for several.
langgraph-checkpoint-redis>=0.0.5

# Retries with backoff for LLM and gRPC calls, instead of hand-rolled sleep
# loops that each get the jitter subtly different.
tenacity>=8.2

# Structured LLM output. Decision schemas are pydantic models with coercing
# validators — see agent/decisions.py for why they coerce rather than raise.
pydantic>=2.6

# Vector search — gRPC to embedding-service. No torch, no
# sentence-transformers, no qdrant-client: embedding-service owns all three.
grpcio>=1.60
protobuf>=4.25
# grpcio-tools is NOT here: stubs are checked in, and regenerating them is a
# development action. `pip install grpcio-tools` before running
# scripts/gen_proto.sh.

# Persistence
sqlalchemy>=2.0
psycopg2-binary>=2.9
alembic>=1.13
redis>=5.0

# API
fastapi>=0.110
uvicorn>=0.29

python-dotenv>=1.0

# test harness only
pytest>=8.0
streamlit>=1.35
requests>=2.31

google-genai>=1.0
